const Component = {
  styleOverrides: {
    root: ({theme}) => ({
      padding: theme.spacing(0.5)
    }),
    skeletonRoot: ({theme}) => ({
      padding: theme.spacing(0.5)
    })
  }
};

export default Component;
