import {Icon} from '@mui/material';
import React from 'react';

const Component = {
  defaultProps: {
    IconComponent: ({className}) => <Icon className={className}>expand_more</Icon>
  }
};

export default Component;
