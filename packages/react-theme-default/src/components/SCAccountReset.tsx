const Component = {
  styleOverrides: {
    root: ({theme}: any) => ({
      '& .MuiAlert-message': {
        display: 'flex',
        alignItems: 'center'
      }
    })
  }
};

export default Component;
