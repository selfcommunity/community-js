const Component = {
  styleOverrides: {
    root: ({theme}: any) => ({
      marginTop: theme.spacing(3),
      '& .SCCategory-root': {
        padding: theme.spacing(1)
      }
    })
  }
};

export default Component;
