const Component = {
  styleOverrides: {
    root: ({theme}: any) => ({
      '& .SCLessonObject-title-section': {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between'
      }
    })
  }
};

export default Component;
