const Component = {
  styleOverrides: {
    displayRoot: ({theme}: any) => ({
      '& .SCMediaEvent-event-preview': {
        margin: theme.spacing()
      }
    })
  }
};

export default Component;
