const Component = {
  styleOverrides: {
    root: ({theme}: any) => ({
      '& h2 .MuiIconButton-root': {
        top: theme.spacing(1),
        fontSize: '1.57rem'
      }
    })
  }
};

export default Component;
