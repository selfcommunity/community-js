const Component = {
  styleOverrides: {
    root: ({theme}) => ({
      borderRadius: '3px'
    }),
    skeletonRoot: ({theme}) => ({
      borderRadius: '3px'
    })
  }
};

export default Component;
