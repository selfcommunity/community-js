const Component = {
  styleOverrides: {
    root: ({theme}: any) => ({}),
    skeletonRoot: ({theme}: any) => ({
      marginBottom: theme.spacing(2)
    }),
    dialogRoot: ({theme}: any) => ({})
  }
};

export default Component;
